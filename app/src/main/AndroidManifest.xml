<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.ufpi.easii.es.vendedistraido">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".view.CadastroActivity"></activity>

        <activity
            android:name=".view.GestorActivity"
            android:label="@string/title_activity_gestor"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".view.CorretorActivity" />
        <activity android:name=".view.CadastraCorretorActivity" />
        <activity android:name=".view.CadastraImovelActivity"></activity>

    </application>

</manifest>